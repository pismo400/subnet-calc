# Subnet Calculator (PWA) — 引き継ぎメモ

iPhoneのホーム画面に追加して使う、オフライン動作のサブネット計算Webアプリ。
GitHub Pagesで公開し、データセンター内などの圏外でも使えることが最重要要件。

- 現行バージョン: **v2.8**
- 公開: GitHub Pages(`main` ブランチ / root)。**Publicリポジトリ**
- ビルド工程なし。依存ライブラリなし。すべて `index.html` 1ファイルに HTML/CSS/JS を記述

## ファイル構成

| ファイル | 役割 |
|---|---|
| `index.html` | アプリ本体(3タブ: Calculator / Reference / Binary) |
| `sw.js` | Service Worker(オフラインキャッシュと更新制御) |
| `manifest.json` | PWA設定 |
| `icon-180.png` / `icon-192.png` / `icon-512.png` | アイコン |
| `CLAUDE.md` | このメモ |

## リリース時の必須ルール

1. **`sw.js` の `VERSION` と `index.html` の `APP_VERSION` を必ず同時に上げる**(例: `2.8` / `'v2.8'`)。
   `VERSION` を変えない限り、端末に更新は一切届かない設計。
2. ファイルを追加・改名したら `sw.js` の `ASSETS` を更新する。1つでも取得に失敗するとインストール全体が失敗し、旧版が使われ続ける(意図した挙動)。
3. コミットメッセージは `vX.Y: 変更内容` の形式。
4. push前に差分をユーザーに提示し、承認を得てからpushする。

## 更新の仕組み(変更時は壊さないこと)

- `install` で `ASSETS` を `cache: 'reload'` で全取得(HTTPキャッシュを回避)。**自動の `skipWaiting` はしない**。
- 新版のインストール完了後、画面下に「新しいバージョンがあります [更新]」バナーを表示。タップ時のみ `SKIP_WAITING` → `controllerchange` で再読み込み。
- `fetch` は現在バージョンのキャッシュ優先。新版への切り替えはキャッシュからの再読み込みなので、圏外でもnot foundにならない。
- 起動時と `visibilitychange`(復帰時)に `reg.update()`。

## 絶対に守る制約

- **オフラインで完結すること。** 外部CDN、外部フォント、解析タグ、外部API呼び出しは追加しない。
- **Publicリポジトリなので、社内情報(IP体系、拠点名、機器名など)を含めない。** 履歴にも残るため一度でもコミットしない。
- `<meta name="robots" content="noindex, nofollow">` を維持。

## UI・文言の決めごと

- 項目名・ラベルは英語の技術用語。**エラーメッセージと解説文は日本語**(誤解防止)。
- 例示は `e.g.` ではなく `ex.`。
- ライト/ダーク両対応。色はCSS変数で管理。ダーク切り替えは全タブで共有し `localStorage` に保存。
- タブバーは「B案」: 不透明な背景、上端の境界線と影、選択中タブは薄い青の角丸で囲み太字。
- 各タブのスクロール末尾にバージョン表記(`Subnet Calc vX.Y`、中央寄せ)。

### Calculator
- 入力欄は `inputmode="decimal"`(テンキー)。区切りは `.` `/` 半角スペース `,` を混在可。全角数字・全角記号は半角に正規化。
- 数値が5個 → IP+プレフィックス長、8個 → IP+サブネットマスク。マスクは連続ビットのみ有効。
- 入力停止2秒後(カーソルが末尾のときのみ)と、フォーカスが外れたときに正規表記へ整形(例: `192.168.1.99.24` → `192.168.1.99/24`、マスク入力は `/255.255.255.0` のまま)。エラー中は整形しない。
- フォーカス時に全選択。
- iOSの連絡先AutoFill対策として、入力欄に `<label>` を関連付けない(見出しは `aria-hidden` の div)。`name="subnet-search"`。Safariは `autocomplete="off"` を無視するため。
- 結果: Network Address(`/prefix` 付き)、Broadcast、Subnet Mask、Total Addresses、Usable Hosts、Binary(ネットワーク部/ホスト部を色分け)。
- /31 は RFC 3021 で2アドレスとも使用可、/32 は1ホストとして注記を表示。
- RESULTS右端のバッジ: `PRIVATE`(緑)/ `GLOBAL`(オレンジ)/ 特殊用途(紫)。判定表は IANA IPv4 Special-Purpose Address Registry、RFC 1918、RFC 6598、マルチキャストは RFC 5771。タップで日本語解説と出典リンクを開閉。範囲が複数区分にまたがる場合は注記。
- タップでコピー。Usable Hostsは範囲行のタップで `先頭-末尾`(半角ハイフン)、/32 は単一アドレス。

### Reference
- `/32` から `/1` の降順。クラス境界(/24, /16, /8 の開始位置)に太線と `Class C/B/A` ラベル(Prefix列の左端に揃える)。
- アドレス数は総数(太字)と利用可能数(小さいグレー、カッコなし)を縦に2段表示。
- Prefixタップで行コピー: `/23 255.255.254.0 512 510`(半角スペース区切り、数値はカンマなし)。Subnet Maskタップでマスクのみコピー。アドレス数セルはコピーしない。

### Binary
- 8ビットのボタン(重み 128〜1)、10進・16進・内訳表示、マスク値プリセット。最後の値を `localStorage` に保存。

## iOS固有の回避策

- iOS 26のホーム画面Webアプリで、WebKitがビューポート高さを上部safe-area分だけ短く報告し、最下部に余白が出る不具合への対策として、`fixViewport()` で差分を検出した場合のみ `--vh-fix` で `.app` の下端を伸ばす。保険として `html, body` の背景をタブバー色にしている。
- **v2.7時点で、この補正が実機で効いたかは未確認。** ユーザーの確認結果を優先すること。

## テスト

- 実機(iPhone)での確認が最終判断。ホーム画面アプリとして、機内モード→完全終了→再起動で起動できることを必ず確認してもらう。
- ロジックは jsdom などで境界値を確認: /0, /1, /8, /15, /16, /23, /24, /31, /32、172.15.255.255 / 172.16.0.1 / 172.32.0.1、100.64.0.0/10 の内外、区分をまたぐ 10.0.0.1/7 など。

## 今後の候補(未着手)

- Binaryタブへの簡易クイズ(出題→回答→答え合わせ)
- ホーム画面アイコンのデザイン変更(変更時はアイコンの再追加が必要な点をユーザーに伝える)
